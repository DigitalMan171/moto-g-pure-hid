name: Ellis Dependency-First Build V9
on: [workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Pull Source
        run: |
          git clone --depth=1 -b android-12-release-s3rhs32.20-42-10-4-11 https://github.com/MotorolaMobilityLLC/kernel-mtk.git .

      - name: 2. Toolchain Setup
        id: cache-toolchain
        uses: actions/cache@v4
        with:
          path: |
            prebuilts/clang/host/linux-x86/clang-r416183b
            prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9
          key: toolchain-ellis-v26-${{ runner.os }}

      - name: 3. Create Custom Fragment with Full Dependency Chain
        run: |
          cat <<EOF > custom_hid.cfg
          # --- Platform & Bus Parents ---
          CONFIG_ARCH_MEDIATEK=y
          CONFIG_MTK_PLATFORM="mt6765"
          CONFIG_ARCH_MT6765=y
          CONFIG_MTK_CMDQ=y
          CONFIG_MTK_SMI=y
          
          # --- Memory Management (Dependent on SMI/CMDQ) ---
          CONFIG_MTK_M4U=y
          CONFIG_MTK_M4U_V2=y
          CONFIG_MTK_IOMMU=y
          CONFIG_MTK_IOMMU_V2=y
          CONFIG_IOMMU_IO_PGTABLE_ARMV8L_FAST=y
          CONFIG_IOMMU_IO_PGTABLE_FAST=y
          
          # --- GKI Visibility Toggles ---
          CONFIG_GKI_HIDDEN_IOMMU_CONFIGS=y
          CONFIG_GKI_HIDDEN_USB_CONFIGS=y
          
          # --- Touchscreen Infrastructure ---
          CONFIG_INPUT_TOUCHSCREEN=y
          CONFIG_I2C_MTK=y
          CONFIG_TOUCHSCREEN_MTK_HIMAX_HX83102P=y
          CONFIG_TOUCHSCREEN_HIMAX_INCELL=y
          
          # --- USB HID Gadget Stack ---
          CONFIG_USB_LIBCOMPOSITE=y
          CONFIG_USB_CONFIGFS=y
          CONFIG_USB_CONFIGFS_F_HID=y
          CONFIG_USB_F_HID=y
          CONFIG_USB_HID=y
          CONFIG_HID_GENERIC=y
          EOF

      - name: 4. Formal Motorola Merge Protocol
        run: |
          export ARCH=arm64
          export KBUILD_OUTPUT=out
          export PATH=$GITHUB_WORKSPACE/prebuilts/clang/host/linux-x86/clang-r416183b/bin:$GITHUB_WORKSPACE/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9/bin:$PATH
          
          mkdir -p out
          
          # We merge in order of increasing specificity. 
          # custom_hid.cfg is last so it has final say over the others.
          bash scripts/kconfig/merge_config.sh -m -r -O out \
            arch/arm64/configs/gki_defconfig \
            arch/arm64/configs/ext_config/moto-mt6765.config \
            arch/arm64/configs/ext_config/ellis_defconfig.config \
            custom_hid.cfg
          
          # Finalize the configuration tree
          make O=$KBUILD_OUTPUT ARCH=arm64 PROJECT=ellis olddefconfig

      - name: 5. Verification Check
        run: |
          echo "Verifying that dependencies allowed drivers to stay..."
          grep "CONFIG_MTK_M4U=y" out/.config || (echo "M4U still missing - logical dependency fail" && exit 1)
          grep "CONFIG_USB_F_HID=y" out/.config || (echo "HID missing" && exit 1)
          echo "Success: Dependency chain is satisfied."

      - name: 6. Compile
        run: |
          export ARCH=arm64
          export KBUILD_OUTPUT=out
          export PATH=$GITHUB_WORKSPACE/prebuilts/clang/host/linux-x86/clang-r416183b/bin:$GITHUB_WORKSPACE/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9/bin:$PATH
          make O=$KBUILD_OUTPUT PROJECT=ellis TARGET_BOARD_PLATFORM=mt6765 KERNEL_DEFCONFIG=ellis_defconfig CC=clang LLVM=1 LLVM_IAS=1 CROSS_COMPILE=aarch64-linux-android- CLANG_TRIPLE=aarch64-linux-gnu- KCFLAGS="-w" -j$(nproc) Image.gz

      - name: 7. Package & Upload
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Ellis-Final-V9
          path: |
            out/arch/arm64/boot/Image.gz
            out/.config
