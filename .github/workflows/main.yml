name: Motorola Ellis Factory Build v28
on: [workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Environment Persistence (v28)
        id: cache-all
        uses: actions/cache@v4
        with:
          path: |
            prebuilts
            external
            kernel
            build
            device
          key: ${{ runner.os }}-moto-ellis-v28

      - name: 2. Sync Parameter Infrastructure
        if: steps.cache-all.outputs.cache-hit != 'true'
        run: |
          # AOSP Build Logic (Trifecta)
          for repo in make soong blueprint; do
            git clone --depth=1 -b android-12.1.0_r27 https://android.googlesource.com/platform/build/$repo build/$repo
          done
          
          # External Dependencies for Soong
          mkdir -p external
          git clone --depth=1 -b android-12.1.0_r27 https://android.googlesource.com/platform/external/golang-protobuf external/golang-protobuf
          
          # Prebuilt Host Tools (Go, Clang, Ninja, Build-Tools)
          git clone --depth=1 https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86 prebuilts/clang/host/linux-x86
          git clone --depth=1 -b android-12.1.0_r27 https://android.googlesource.com/platform/prebuilts/go/linux-x86 prebuilts/go/linux-x86
          git clone --depth=1 -b android-12.1.0_r27 https://android.googlesource.com/platform/prebuilts/ninja/linux-x86 prebuilts/ninja/linux-x86
          git clone --depth=1 -b android-12.1.0_r27 https://android.googlesource.com/platform/prebuilts/build-tools prebuilts/build-tools
          
          # Establish standard root entry point
          ln -s make/envsetup.sh build/envsetup.sh
          
          # Motorola Kernel Source
          git clone --depth=1 -b android-12-release-s3rhs32.20-42-10-4-11 https://github.com/MotorolaMobilityLLC/kernel-mtk.git kernel

      - name: 3. Hardware Identity (Device Tree)
        run: |
          mkdir -p device/motorola/ellis
          # Map the lunch combo to the product makefile
          cat <<EOF > device/motorola/ellis/AndroidProducts.mk
          PRODUCT_MAKEFILES := \$(LOCAL_DIR)/full_ellis.mk
          COMMON_LUNCH_CHOICES := full_ellis-userdebug
          EOF

          cat <<EOF > device/motorola/ellis/full_ellis.mk
          \$(call inherit-product, \$(SRC_TARGET_DIR)/product/core_64_bit.mk)
          \$(call inherit-product, \$(SRC_TARGET_DIR)/product/full_base.mk)
          PRODUCT_DEVICE := ellis
          PRODUCT_NAME := full_ellis
          EOF

      - name: 4. Execution (Factory Lunch & Parameter Build)
        run: |
          export TOP=$(pwd)
          
          # Set execution bits for the prebuilt toolchain
          find prebuilts -type f -executable -exec chmod +x {} + 2>/dev/null || true
          
          # Source the environment to initialize the 'lunch' function
          source build/envsetup.sh
          
          # Apply the Ellis parameters to the shell
          lunch full_ellis-userdebug
          
          # Extract the specific Clang version for the Kernel Kbuild
          CLANG_BIN=$(ls -d $GITHUB_WORKSPACE/prebuilts/clang/host/linux-x86/clang-r*/bin | sort -V | tail -n 1)
          export PATH="$CLANG_BIN:$PATH"
          
          # Build specifically in the kernel context with the applied parameters
          cd kernel
          KBUILD_ARGS="-j$(nproc) O=$TOP/out ARCH=arm64 LLVM=1 LLVM_IAS=1 CROSS_COMPILE=aarch64-linux-gnu- CLANG_TRIPLE=aarch64-linux-gnu-"
          
          # 1. Initialize defconfig
          make $KBUILD_ARGS ellis_defconfig
          
          # 2. Set Project-Specific Parameters (HID)
          ../kernel/scripts/config --file $TOP/out/.config --enable CONFIG_UHID
          ../kernel/scripts/config --file $TOP/out/.config --enable CONFIG_USB_HID
          ../kernel/scripts/config --file $TOP/out/.config --enable CONFIG_HID_GENERIC
          
          # 3. Compile
          make $KBUILD_ARGS

      - name: 5. Archive Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: moto-g-pure-ellis-v28
          path: |
            out/arch/arm64/boot/Image.gz*
            out/.config
