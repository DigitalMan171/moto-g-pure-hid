name: Ellis Infrastructure-First V28
on: [workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Pull Source
        run: |
          git clone --depth=1 -b android-12-release-s3rhs32.20-42-10-4-11 https://github.com/MotorolaMobilityLLC/kernel-mtk.git .

      - name: 2. Toolchain Setup
        id: cache-toolchain
        uses: actions/cache@v4
        with:
          path: |
            prebuilts/clang/host/linux-x86/clang-r416183b
            prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9
          key: toolchain-ellis-v35-${{ runner.os }}

      - name: 3. Create Infrastructure Fragment
        run: |
          cat <<EOF > final_override.cfg
          # --- Platform Identity & Architecture ---
          CONFIG_ARCH_MEDIATEK=y
          CONFIG_MTK_PLATFORM="mt6765"
          CONFIG_ARCH_MT6765=y
          CONFIG_MTK_PLATFORM_MT6765=y
          
          # --- The "Glue" Infrastructure (Missing Chain Links) ---
          CONFIG_MTK_PMIC_WRAP=y
          CONFIG_MTK_SCP=y
          CONFIG_MTK_SMI=y
          CONFIG_MTK_SMI_COMMON=y
          
          # --- Motorola Developer Unlocks ---
          CONFIG_MEDIATEK_SOLUTION=y
          CONFIG_MTK_ENG_BUILD=y
          CONFIG_MOTOROLA_LOCKED_GKI=n
          
          # --- HID & USB ---
          CONFIG_MTK_USB_GADGET=y
          CONFIG_USB_F_HID=y
          CONFIG_USB_CONFIGFS_F_HID=y
          CONFIG_USB_HID=y
          CONFIG_HID_GENERIC=y
          
          # --- Mandatory Power/Clock ---
          CONFIG_COMMON_CLK_MT6765=y
          CONFIG_MTK_PMIC_WRAP_MT6765=y
          EOF

      - name: 4. The Correct Merge Order
        run: |
          export ARCH=arm64
          export KBUILD_OUTPUT=out
          export PATH=$GITHUB_WORKSPACE/prebuilts/clang/host/linux-x86/clang-r416183b/bin:$GITHUB_WORKSPACE/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9/bin:$PATH
          
          mkdir -p out
          
          # GKI base -> Moto Platform -> Ellis Specific -> Our Overrides
          bash scripts/kconfig/merge_config.sh -m -O out \
            arch/arm64/configs/gki_defconfig \
            arch/arm64/configs/ext_config/moto-mt6765.config \
            arch/arm64/configs/ext_config/ellis_defconfig.config \
            final_override.cfg
          
          # Settle dependencies - strictly identifying the project
          make O=$KBUILD_OUTPUT ARCH=arm64 PROJECT=ellis olddefconfig

      - name: 5. Verification
        run: |
          echo "Verifying Hardware Chain Survival..."
          grep "CONFIG_ARCH_MEDIATEK=y" out/.config || (echo "ARCH LOST" && exit 1)
          grep "CONFIG_MTK_PMIC_WRAP=y" out/.config || (echo "PMIC_WRAP LOST (Bridge Break)" && exit 1)
          grep "CONFIG_MEDIATEK_SOLUTION=y" out/.config || (echo "VENDOR SOLUTION STILL STRIPPED" && exit 1)
          grep "CONFIG_USB_F_HID=y" out/.config || (echo "HID LOST" && exit 1)
          echo "Chain intact."

      - name: 6. Compile
        run: |
          export ARCH=arm64
          export KBUILD_OUTPUT=out
          export PATH=$GITHUB_WORKSPACE/prebuilts/clang/host/linux-x86/clang-r416183b/bin:$GITHUB_WORKSPACE/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9/bin:$PATH
          make O=$KBUILD_OUTPUT PROJECT=ellis TARGET_BOARD_PLATFORM=mt6765 CC=clang LLVM=1 LLVM_IAS=1 CROSS_COMPILE=aarch64-linux-android- CLANG_TRIPLE=aarch64-linux-gnu- KCFLAGS="-w" -j$(nproc) Image.gz

      - name: 7. Package & Upload
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Ellis-Infrastructure-V28
          path: |
            out/arch/arm64/boot/Image.gz
            out/.config
